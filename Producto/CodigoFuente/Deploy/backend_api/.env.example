ENVIRONMENT=development

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
APP_TIMEZONE=America/Santiago

SUPABASE_KEY=

ANTHROPIC_API_KEY=
# El modelo del OCR esta fijado en codigo a claude-sonnet-5 (core/config.py).
# Estas variables se mantienen como referencia; ya no alteran el modelo usado.
ANTHROPIC_MODEL=claude-sonnet-5
ANTHROPIC_OCR_AUDIT_MODEL=claude-sonnet-5

# Modo del motor OCR: "segmented" (default, pipeline de recortes/contact sheets)
# o "full_rectangle" (envia a Claude una sola imagen del rectangulo rectificado y rotado).
OCR_MODE=segmented

JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
TRUST_PROXY_HEADERS=false

ALLOWED_ORIGINS=http://localhost:5173,https://localhost:5173,http://localhost:5174,https://localhost:5174,http://127.0.0.1:5174,https://127.0.0.1:5174,http://localhost:3000,https://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

HOST=127.0.0.1
PORT=8000
HTTPS_ENABLED=false
SSL_CERTFILE=../../certs/dev/bluegridocr.local.pem
SSL_KEYFILE=../../certs/dev/bluegridocr.local.key

# Azure/Docker production notes:
# ENVIRONMENT=production
# HOST=0.0.0.0
# HTTPS_ENABLED=false
# ALLOWED_ORIGINS=https://tu-app.azurewebsites.net

NGROK_TOKEN=
